# Criterion: The Day View correctly identifies and highlights all intervals of 5 minutes
# or longer between logged entries as untracked gaps.
# TODO Phase 3: This test requires seeding time entries with synthetic data (e.g. a
# completed entry ending at T and a second entry starting at T+6min) to verify gap
# detection. Currently we can only verify the UI renders gap rows — not the threshold
# logic — without a test data seeding mechanism.
appId: com.boa.clientclock
---
- launchApp:
    clearState: true
- tapOn: "Skip"
# Subscribe to unlock Day View
# TODO Phase 3: replace with proper subscription mock
- tapOn: "Today"
- assertVisible: "Today's timeline"
- takeScreenshot: "04-gap-detection"
