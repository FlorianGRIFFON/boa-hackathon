{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Structured output schema for the brainstorm agent. Every field is required — the agent must not proceed without answering all of them.",
  "properties": {
    "app_name": {
      "type": "string",
      "description": "Short, memorable app name (2-3 words max)"
    },
    "problem_statement": {
      "type": "string",
      "description": "The specific problem this app solves, written as a user story: 'When I [situation], I struggle to [problem] because [root cause]'"
    },
    "target_user": {
      "type": "string",
      "description": "Concrete description of the primary user. Not a demographic — a person with a specific context and need."
    },
    "why_existing_solutions_fail": {
      "type": "string",
      "description": "What does the user try today, and why is it not good enough? Must name at least one real alternative."
    },
    "hero_feature": {
      "type": "string",
      "description": "The single feature that defines this app's value. One sentence."
    },
    "out_of_scope": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 3,
      "description": "Features explicitly excluded from the MVP. These are guardrails for the builder agent."
    },
    "mvp_screens": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 2,
      "maxItems": 5,
      "description": "Exhaustive list of screens in the MVP. The builder agent will build exactly these — no more."
    },
    "success_criteria": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 3,
      "description": "Testable acceptance criteria. Each must be verifiable by the QA agent without human judgment."
    }
  },
  "required": [
    "app_name",
    "problem_statement",
    "target_user",
    "why_existing_solutions_fail",
    "hero_feature",
    "out_of_scope",
    "mvp_screens",
    "success_criteria"
  ]
}
